# cat=access/licence; type=options[basic,economy,business]; label=DocCheck Access licence mode: Select the licence assigned to the DocCheck Access client. Basic authenticates only and never sends scopes or state.
licenseMode = basic

# cat=access/scopes; type=string; label=Requested DocCheck Access scopes: Economy and Business only. Leave empty for Basic; frontend-user provisioning requires unique_id.
requestedScopes =

# cat=frontend-users/provisioning; type=boolean; label=Enable DocCheck Access frontend-user provisioning: Economy and Business only. Requires the unique_id scope.
enableFrontendUserProvisioning = 0

# cat=frontend-users/provisioning; type=int; label=Default TYPO3 frontend-user group UID: Used only when frontend-user provisioning is enabled. 0 assigns no group.
defaultFrontendUserGroup = 0

# cat=frontend-users/provisioning; type=string; label=Persist approved DocCheck Access profile fields: Business only. Requires frontend-user provisioning and matching requested scopes; writes only newly created FE-user records.
profileFieldMapping =

# cat=frontend-users/provisioning; type=options[create_only]; label=DocCheck Access profile-field synchronization: Currently, create_only is the only available and safe policy. Local name/email edits are never overwritten by a later DocCheck Access login.
# Currently, create_only is the only available and safe policy: local name/email edits are never overwritten by a later DocCheck Access login.
profileFieldSync = create_only

# cat=frontend-users/fallback; type=boolean; label=Allow anonymous DocCheck Access session fallback: Economy and Business only. Disabled by default so frontend-user provisioning failures do not grant access.
allowAnonymousSessionFallback = 0

# cat=diagnostics/logging; type=boolean; label=Enable DocCheck Access diagnostic logging: Development only. Logs scope/status/missing API field names, never profile values, tokens, credentials, or callback parameters.
debugLogging = 0
